apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sentinelvision-models
  namespace: sentinelvision
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
